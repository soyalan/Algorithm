#include <iostream>
#include <vector>

using namespace std;

int main() {

	int M, N, K;
	cin >> M >> N >> K;
	vector<int> a, b, c;
	for (int i = 1; i <= N; i++) a.push_back(i);

	if (M == 1) {
		int cnt = 0;
		while (cnt < K) {

		}
	}
	else if (M == 2) {

	}
	else {

	}

	return 0;
}
